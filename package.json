{
  "name": "euno-app",
  "version": "1.3.6",
  "private": true,
  "description": "EUNO Autopilot – Capacitor app",
  "scripts": {
    "copy": "npx cap copy",
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "serve": "npx http-server web -p 5173 -c-1"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/network": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "http-server": "^14.1.1"
  }
}
